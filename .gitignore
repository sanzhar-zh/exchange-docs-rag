.env
.venv/
venv/
__pycache__/
*.pyc

# Third-party documentation, fetched by scripts/fetch_docs.py.
# Not redistributed here: it belongs to the exchanges.
data/raw/

# Generated index. Rebuilt from data/raw by ingest.py.
data/chroma/

# Tool caches. pytest and ruff each write a self-ignoring .gitignore of their
# own; this is here so that stays true if either stops doing it.
.pytest_cache/
.ruff_cache/
